<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="employee">

	<select id="loginCheck" resultMap="empMap" parameterType="empvo">
		select
		* from employee
		where emp_no = #{empNo}
		and emp_pw = #{empPw}
	</select>
	
	<insert id="joinEmployee" parameterType="empvo">
		insert into
		employee(emp_no, emp_pw, emp_addr, emp_tel, emp_email, emp_name, emp_posit, admin_code, dept_code)
		values
		(#{empNo}, #{empPw}, #{empAddr}, #{empTel}, #{empEmail},
		#{empName}, #{empPosit}, #{adminCode}, #{deptCode})
	</insert>
	
	<update id="updateEmployee" parameterType="empvo">
		update employee
		set emp_pw = #{empPw},
			emp_addr = #{empAddr},
			emp_tel = #{empTel},
			emp_email = #{empEmail},
			emp_name = #{empName},
			emp_posit = #{empPosit},
			state_Code = #{stateCode},
			admin_code = #{adminCode},
			dept_code = #{deptCode}
			where emp_no = #{empNo}
	</update>

	<delete id="deleteEmployee" parameterType="String">
		delete from EMPLOYEE
		where emp_no = #{empNo}
	</delete>

	<select id="checkEmployee" parameterType="String" resultType="int">
		select count(*) as cnt
		from EMPLOYEE
		where emp_no = #{empNo}
	</select>
	
	<select id="getEmployee" resultMap="empMap">
		select * from employee
		where emp_no = #{empNo}
	</select>

	<select id="selectAll" resultMap="empMap">
		select * from EMPLOYEE
		order by emp_no ${desc}
	</select>

	<resultMap type="vo.EmpVO" id="empMap">
		<result column="emp_no" property="empNo" />
		<result column="emp_pw" property="empPw" />
		<result column="emp_addr" property="empAddr" />
		<result column="emp_tel" property="empTel" />
		<result column="emp_email" property="empEmail" />
		<result column="emp_name" property="empName" />
		<result column="emp_posit" property="empPosit" />
		<result column="state_code" property="stateCode" />
		<result column="admin_code" property="adminCode" />
		<result column="dept_code" property="deptCode" />
	</resultMap>

</mapper>